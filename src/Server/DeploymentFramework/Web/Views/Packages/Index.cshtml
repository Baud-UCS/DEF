@using Baud.Deployment.Resources

@{
    ViewBag.Title = @StringResources.Packages;
}

<h2>
    @StringResources.Packages
</h2>

<p>
    @StringResources.PackagesUploadInfo <i>@Helpers.GetPushUrl(Request.Url, Request.ApplicationPath)</i>
</p>
<p>
    @StringResources.RunningNugetServer v@(typeof(NuGet.Server.DataServices.Package).Assembly.GetName().Version)
</p>